// Phasor: sawtooth ramp from 0.0 to 1.0 at a given frequency in Hz
// Usage: val phase = phasor(freq_hz);
fun phasor(freq:real) : real {
  mem phase;
  phase = phase + freq / 44100.0;
  phase = if phase >= 1.0 then phase - 1.0 else phase;
  return phase;
}
